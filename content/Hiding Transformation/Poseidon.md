Link for info: https://www.poseidon-hash.info/. It also has links to implementations
# About Poseidon Hash

# Using Poseidon as Sponge

# Poseidon PRNG
*these claims are via superficial understanding*
Suppose we want k many random number
- The Execution Trace size scales linearly with k (O(k)).
- The provers compute requirement scales linearly with k (O(k)).
- The final proof size does not scale - only 1 Polynomial Commit over all random numbers ig

